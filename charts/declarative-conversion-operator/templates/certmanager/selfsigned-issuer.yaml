{{- if .Values.certManager.selfSigned.enabled }}
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: {{ include "declarative-conversion-operator.fullname" . }}-selfsigned-issuer
  labels:
    {{- include "declarative-conversion-operator.labels" . | nindent 4 }}
spec:
  selfSigned: {}
{{- end }}
