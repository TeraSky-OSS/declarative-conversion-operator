{{- if .Values.certManager.selfSigned.enabled }}
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: {{ include "xrd-conversion-operator.fullname" . }}-selfsigned-issuer
  labels:
    {{- include "xrd-conversion-operator.labels" . | nindent 4 }}
spec:
  selfSigned: {}
{{- end }}
